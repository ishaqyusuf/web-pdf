# Database Schema

## Purpose
This file tracks database or persisted schema concerns if `web-pdf` introduces storage-backed features in the future.

## How To Use
- Update only when the project adds persistence.
- Keep current status clear if no database exists yet.
- Link migrations and relationships docs when storage is introduced.

## Template

### Current State
- No database schema defined yet
- Current project scope is package and runtime tooling, not persisted application data

### Future Use Cases
- Template marketplace metadata
- Visual editor persistence
- User-defined document schemas
- Registry or analytics data if a hosted product emerges
